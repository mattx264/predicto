# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main" ]
    paths-ignore: ['./app/**']
  pull_request:
    branches: [ "main" ]
jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET    
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x
    - name: Restore dependencies
      working-directory: ./api
      run: dotnet restore
    - name: Build
      working-directory: ./api
      run: dotnet build --no-restore
    - name: Publish
      working-directory: ./api
      run: dotnet publish
    - name: Deploy using WebDeploy (msdeploy)
      run: |
        $publish = "${{ github.workspace }}\publish"
        & "C:\Program Files\IIS\Microsoft Web Deploy V3\msdeploy.exe" -source:contentPath="$publish" -dest:contentPath=mattx2642-001-site26,ComputerName=https://win8210.site4now.net:8172/msdeploy.axd?site=mattx2642-001-site26,UserName=${{ secrets.WEBDEPLOY_USERNAME }},Password=${{ secrets.WEBDEPLOY_PASSWORD }},AuthType=Basic,IncludeAcls=False -enableRule:AppOffline -verb:sync -allowUntrusted   
